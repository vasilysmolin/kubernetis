apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bb-ssl-volume-claim
  namespace: production
spec:
  storageClassName: s3-beautybox
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
